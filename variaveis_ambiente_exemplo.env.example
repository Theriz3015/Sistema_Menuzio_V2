# Variáveis de Ambiente para o Backend do Projeto Menuzio

# Configuração da Porta do Servidor
PORT=3000

# URI de Conexão com o Banco de Dados MongoDB
# Exemplo para MongoDB local: mongodb://localhost:27017/menuzio_bd
# Exemplo para MongoDB Atlas: mongodb+srv://<user>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority
MONGO_URL=mongodb://localhost:27017/menuzio_bd

# Chave Secreta para Geração e Verificação de JSON Web Tokens (JWT)
# Use uma string complexa e única para segurança.
JWT_SECRET=sua_chave_secreta_super_segura_aqui

# Tempo de Expiração do JWT
# Exemplo: '1h' (1 hora), '7d' (7 dias)
JWT_EXPIRES_IN=1h

# Diretório para uploads (ex: fotos de perfil, se implementado)
# Pode ser um caminho relativo ou absoluto dentro do projeto
# UPLOAD_DIR=uploads/